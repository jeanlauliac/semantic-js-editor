import enumerate from '../utils/enumerate'

var BinaryOpType = enumerate('BinaryOpType', {
  ADD: null,
  SUBTRACT: null,
  MULTIPLY: null,
  DIVIDE: null,
  MODULO: null,
})

export default BinaryOpType
