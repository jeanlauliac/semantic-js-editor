import transformify from './utils/transformify'

var Styles = stylify({
  '.root': {
    margin: '0 auto',
    maxWidth: '32rem',
  },
})

export default ApplicationView = React.createClass({
  propTypes: {
    /**
     * The application data.
     */
    initialApplication: React.PropTypes.string,
  },

})
